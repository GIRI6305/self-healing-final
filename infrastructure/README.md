# Infrastructure
This local-safe Terraform module documents environment configuration without creating paid AWS resources. AWS modules can be added later behind explicit variables and provider configuration.
