global:
  scrape_interval: 15s

rule_files:
  - "/etc/prometheus/rules/*.yml"

scrape_configs:
  - job_name: platform-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - "platform-api:8080"
