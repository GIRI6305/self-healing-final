# Testing
Unit/context tests are under `backend/src/test`. The master validation script runs Maven tests, static configuration checks, image/security checks when available, and live health/API checks when the application is reachable.
