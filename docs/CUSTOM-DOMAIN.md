# Custom Domain

The application is prepared for a custom domain.

Recommended structure:

app.<your-domain>
api.<your-domain>

The actual domain must be registered and configured in Vercel/Render and DNS.
