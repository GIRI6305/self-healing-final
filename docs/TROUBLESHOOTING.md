# Troubleshooting
Check `kubectl get pods`, `kubectl describe pod`, `kubectl logs`, and `kubectl get events`. If an optional security tool is absent, the validation script reports SKIPPED. If the Kubernetes context is unavailable, application-only checks can still run.
